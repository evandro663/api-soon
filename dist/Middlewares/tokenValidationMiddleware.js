"use strict";
// import { Request, Response, NextFunction } from 'express';
// import loginService from '../services/loginService';
// const tokenValidationMiddleware = async (req: Request, res: Response, next: NextFunction) => {
//   const token = req.headers.authorization;
//   const validator = await loginService.validateAuthorization(token);
//   await loginService.readToken(validator);
//   next();
// };
// export default tokenValidationMiddleware;
